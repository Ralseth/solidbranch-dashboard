import Dashboard from './components/Dashboard';

export default function App() {
    return (
        <div>
            <h1 className="text-3xl font-bold text-center mt-10">
                Hello, minimal Vite + React + TS!
            </h1>

            <Dashboard />
        </div>
    );
}
